apiVersion: v1
kind: Secret
metadata:
  name: llamastack-credentials
  namespace: agentic-demo
type: Opaque
stringData:
  # vLLM bearer token if your server enforces one; many in-cluster installs accept any value — "fake" is fine.
  vllm-api-token: "fake"
