apiVersion: v1
kind: Secret
metadata:
  name: llamastack-credentials
  namespace: agentic-demo
type: Opaque
stringData:
  # Replace with your MiniMax platform API key before applying.
  minimax-api-key: "REPLACE_WITH_MINIMAX_API_KEY"
