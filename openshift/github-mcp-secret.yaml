apiVersion: v1
kind: Secret
metadata:
  name: github-mcp-pat
  namespace: agentic-demo
type: Opaque
stringData:
  # Fine-grained or classic PAT with the scopes your toolsets need — see GitHub MCP Server README.
  GITHUB_PERSONAL_ACCESS_TOKEN: "REPLACE_WITH_GITHUB_PAT"
